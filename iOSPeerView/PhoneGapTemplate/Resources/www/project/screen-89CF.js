/*
 * JS for Survey Screen generated by Exadel Tiggzi
 *
 * Created on: Tuesday, February 07, 2012, 10:14:49 AM (PST)
 */
/************************************
 * JS API provided by Exadel Tiggzi  *
 ************************************/
/* Setting project environment indicator */
Tiggr.env = "apk"; /* Object & array with components "name-to-id" mapping */
var n2id_buf = {};
if ("n2id" in window && n2id != undefined) {
    $.extend(n2id, n2id_buf);
} else {
    var n2id = n2id_buf;
}

function pageItem(pageName, pageLocation) {
    this.name = pageName;
    this.location = pageLocation;
}
Tiggr.AppPages = [
new pageItem('Register Account Screen', 'screen-0E78.html'), new pageItem('Login Screen', 'screen-EE4B.html'), new pageItem('Survey Screen', 'screen-89CF.html'), new pageItem('Home Screen', 'screen-893A.html'), new pageItem('current', 'screen-89CF.html')];

function navigateTo(outcome, useAjax) {
    Tiggr.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Tiggr.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Tiggr.adjustContentHeightWithPadding();
}

function unwrapAndApply(selector, content) {
    Tiggr.unwrapAndApply(selector, content);
}

function setDetailContent(pageUrl) {
    Tiggr.setDetailContent(pageUrl);
}
/*********************
 * GENERIC SERVICES  *
 *********************/
/*************************
 * NONVISUAL COMPONENTS  *
 *************************/
var datasources = [];
/************************
 * EVENTS AND HANDLERS  *
 ************************/
// screen onload
screen_89CF_onLoad = j_30_onLoad = function() {
    createSpinner("res/lib/jquerymobile/images/ajax-loader.gif");
    for (var idx = 0; idx < datasources.length; idx++) {
        datasources[idx].__setupDisplay();
    }
    adjustContentHeightWithPadding();
}
// screen window events
screen_89CF_windowEvents = j_30_windowEvents = function() {}
// screen elements extra js
screen_89CF_elementsExtraJS = j_30_elementsExtraJS = function() {
    // screen (screen-89CF) extra code
}
// screen elements handler
screen_89CF_elementsEvents = j_30_elementsEvents = function() {
    $("a :input,a a,a fieldset label").live({
        click: function(event) {
            event.stopPropagation();
        }
    });
}
$("body").undelegate("pagebeforeshow").delegate("#j_30", "pagebeforeshow", function(event, ui) {
    j_30_onLoad();
    j_30_windowEvents();
    screen_89CF_elementsExtraJS();
    screen_89CF_elementsEvents();
});
document.addEventListener("deviceready", adjustContentHeightWithPadding);